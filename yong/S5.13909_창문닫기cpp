#include <iostream>

using namespace std;

int main(void){
    int N;
    int count = 0;
    cin >> N;

    int x = 1;
    while(true){
        if(x*x <= N && (x+1)*(x+1) > N){
            count = x;
            break;
        }
        x++;
    }

    cout << count << endl;

    return 0;
}
